import { Decider } from '../source/js/Decider'
require('chai').should()

// No tests because it is a random number

describe('Productivity score', () => {
  const decider = new Decider()

  // it('is random between 1 and 100')

})